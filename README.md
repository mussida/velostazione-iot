# Velostazione IoT

Repository dedicata alla parte IoT del progetto Velostazione di Bologna.

Contiene:

- firmware dei nodi di stallo (Heltec + RC522 + buzzer);
- firmware del gateway LoRa RX;
- servizi Python eseguiti su Raspberry Pi (gateway LoRa → HTTP e API per il desk RFID).
